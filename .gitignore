# Credentials and secrets — never commit these
config.json
config.txt

# Sensor log — large over time and machine-specific
litter_log.csv

# macOS metadata
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
